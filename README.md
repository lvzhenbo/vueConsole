# VueConsole

<div align="center">
  <h3>基于 Vue 3.5 + TypeScript 的移动端调试工具</h3>
  <p>参考 vConsole，使用现代化技术栈重新实现</p>
</div>

## ✨ 特性

- 🎯 **完整功能** - 参考 vConsole 实现所有核心功能
- 🚀 **Vue 3.5** - 使用最新的 Vue 3.5 Composition API
- 📘 **TypeScript** - 完整的类型定义支持
- 🎨 **现代化UI** - 美观的用户界面，支持亮色/暗色主题
- 📱 **移动优先** - 专为移动端优化的交互体验
- 🔌 **双打包机制** - Vite 预览 + Vite 库打包（ESM格式）
- 🌐 **纯中文** - 全中文界面和文档

## 📦 功能模块

### 1. 日志面板 (Log)
- ✅ 捕获 `console.log`、`console.info`、`console.warn`、`console.error`、`console.debug`
- ✅ 支持多参数日志显示
- ✅ 对象、数组、函数等类型的格式化显示
- ✅ 时间戳显示
- ✅ 日志重复计数
- ✅ 命令行执行 JavaScript 代码
- ✅ 自动滚动到底部
- ✅ 日志清空功能

### 2. 系统面板 (System)
- ✅ 设备信息（分辨率、像素比、平台等）
- ✅ 浏览器信息
- ✅ 存储信息（LocalStorage、SessionStorage、Cookies）
- ✅ 性能信息（JS堆内存使用情况）

### 3. 网络面板 (Network)
- ✅ 拦截 XMLHttpRequest 请求
- ✅ 拦截 Fetch 请求
- ✅ 显示请求方法、URL、状态码
- ✅ 显示请求耗时
- ✅ 查看请求头和响应头
- ✅ 查看请求数据和响应数据
- ✅ 网络请求清空功能

### 4. 元素面板 (Element)
- ✅ DOM 树结构查看
- ✅ 展开/折叠节点
- ✅ 元素详情查看（标签、ID、类名、属性等）
- ✅ 刷新 DOM 树
- ✅ 全部展开/收起功能

### 5. 存储面板 (Storage)
- ✅ Cookies 管理（查看、删除）
- ✅ LocalStorage 管理（查看、删除、清空）
- ✅ SessionStorage 管理（查看、删除、清空）
- ✅ 刷新存储列表

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

在浏览器中打开 http://localhost:3000 查看演示页面。

### 构建库

```bash
npm run build
```

构建产物：
- `dist/index.mjs` - ESM 格式的库文件
- `dist/index.css` - 样式文件
- `dist/index.d.ts` - TypeScript 类型定义

## 📖 使用方法

### 基础使用

```typescript
import { VConsole } from 'vue-console'
import 'vue-console/style.css'

// 创建实例
const vConsole = new VConsole({
  theme: 'light' // 或 'dark'
})

// 在页面中会出现一个"调试"按钮，点击即可打开调试面板
```

### 在 Vue 项目中使用

```typescript
import { createApp } from 'vue'
import App from './App.vue'
import VConsole from 'vue-console'
import 'vue-console/style.css'

const app = createApp(App)

// 初始化 VueConsole
new VConsole({
  theme: 'light'
})

app.mount('#app')
```

## 🎯 技术栈

- **框架**: Vue 3.5.13
- **语言**: TypeScript 5.7.2
- **构建工具**: 
  - Vite 6.0.3 (开发服务器)
  - tsdown 0.18.0 (库打包)
  - vue-tsc 2.2.0 (类型声明生成)
- **打包格式**: ESM (ES Module)

## 📂 项目结构

```
vueConsole/
├── src/                 # 库源代码
│   ├── components/       # Vue 组件
│   │   ├── VueConsole.vue   # 主组件
│   │   └── TreeNode.vue     # DOM 树节点组件
│   ├── core/            # 核心功能
│   │   ├── logger.ts        # 日志拦截器
│   │   └── network.ts       # 网络拦截器
│   ├── panels/          # 功能面板
│   │   ├── LogPanel.vue     # 日志面板
│   │   ├── SystemPanel.vue  # 系统面板
│   │   ├── NetworkPanel.vue # 网络面板
│   │   ├── ElementPanel.vue # 元素面板
│   │   └── StoragePanel.vue # 存储面板
│   ├── types/           # 类型定义
│   │   └── index.ts
│   ├── utils/           # 工具函数
│   │   └── index.ts
│   └── index.ts         # 库入口文件
├── examples/            # 示例应用
│   ├── App.vue          # 演示应用组件
│   └── main.ts          # 演示应用入口
├── dist/                # 构建输出
├── index.html           # 演示页面
├── vite.config.ts       # Vite 配置
├── tsconfig.json        # TypeScript 配置
└── package.json         # 项目配置
```

## 🎨 界面展示

### 初始界面
![初始界面](https://github.com/user-attachments/assets/0e1f1fbc-5133-4758-ac0d-3ab6cc52e166)

### 调试面板打开
![调试面板](https://github.com/user-attachments/assets/6ae8d24d-f060-4607-a5a1-cd152baff336)

### 日志面板
![日志面板](https://github.com/user-attachments/assets/5502d255-1b22-4d79-81c0-d6eddff30df0)

### 系统面板
![系统面板](https://github.com/user-attachments/assets/4458ff2e-66d1-4a48-9950-672a8102cc58)

## 🔧 配置选项

```typescript
interface VueConsoleOptions {
  theme?: 'light' | 'dark'              // 主题，默认 'light'
  defaultPlugins?: string[]             // 默认插件列表
  maxLogNumber?: number                 // 最大日志数量，默认 1000
  disableLogScrolling?: boolean         // 禁用日志自动滚动
  target?: HTMLElement | string         // 挂载目标元素，默认 document.body
}
```

### 在嵌入页面（iframe）中使用

当在 iframe 或其他嵌入式环境中使用 VueConsole 时，可以通过 `target` 选项指定挂载容器：

```typescript
// 使用选择器字符串
new VConsole({
  target: '#my-container'
})

// 或使用 DOM 元素
new VConsole({
  target: document.getElementById('my-container')
})
```

## ✅ 特性对比

| 功能 | vConsole | VueConsole |
|-----|----------|------------|
| 日志捕获 | ✅ | ✅ |
| 网络监控 | ✅ | ✅ |
| 元素查看 | ✅ | ✅ |
| 存储管理 | ✅ | ✅ |
| 系统信息 | ✅ | ✅ |
| 暗色主题 | ✅ | ✅ |
| 可拖拽按钮 | ✅ | ✅ |
| 命令行执行 | ✅ | ✅ |
| TypeScript | ❌ | ✅ |
| Vue 3.5 | ❌ | ✅ |
| ESM 打包 | ✅ | ✅ |

## 📝 开发说明

### 添加新功能面板

1. 在 `src/panels/` 目录下创建新的 Vue 组件
2. 在 `src/components/VueConsole.vue` 中注册面板
3. 添加对应的类型定义

### 自定义样式

所有组件使用 CSS 变量，可以通过覆盖变量来自定义样式：

```css
.vue-console {
  --primary-color: #42b983;
  --bg-color: #ffffff;
  --text-color: #2c3e50;
  --border-color: #e0e0e0;
  --hover-color: #f5f5f5;
  --log-bg: #fafafa;
}
```

### 安全说明

⚠️ **注意**：日志面板的命令行功能使用了 `eval()` 来执行 JavaScript 代码。这是调试工具的预期功能（类似浏览器控制台），但在生产环境中请确保：
1. 只在开发/测试环境启用 VueConsole
2. 不要让最终用户访问到调试面板
3. 构建生产版本时移除或禁用 VueConsole

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

## 🙏 致谢

本项目参考了 [vConsole](https://github.com/Tencent/vConsole) 的设计和功能，感谢 Tencent 团队的优秀工作。